/* Shared landing page types for the Landing Page undang-io mockup. */

export type LandingTheme = {
  id: string;
  name: string;
  slug: string;
  thumbnailUrl: string | null;
  culturalCategory: string | null;
};
